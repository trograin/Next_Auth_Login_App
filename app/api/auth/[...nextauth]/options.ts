import type { NextAuthOptions } from "next-auth";
import GitHubProvider from 'next-auth/providers/github';

// Setting up our Providers
// We will use NextAuth own pages
export const options: NextAuthOptions = {
    providers: [],

}